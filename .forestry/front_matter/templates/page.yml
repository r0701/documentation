---
label: Page
hide_body: false
display_field: title
fields:
- name: title
  type: include
  config: {}
  template: page
  label: Title
- type: select
  name: menu
  config:
    source:
      type: simple
    options:
    - main
  label: Menu
